# تجهيز الموقع للنشر

## الخيار المقترح: GitHub Pages
1. أنشئ حساباً في GitHub.
2. أنشئ مستودعاً عاماً جديداً، مثلاً: `jam3iya-en-mnas`.
3. ارفع جميع ملفات هذا المجلد.
4. من Settings → Pages اختر النشر من الفرع `main`.
5. أضف اسم النطاق الحقيقي في خانة Custom domain.
6. أنشئ سجلات DNS المطلوبة لدى شركة الدومين.
7. فعّل HTTPS بعد اكتمال التحقق.

## اسم النطاق
استبدل `www.example.dz` في `CNAME.example` باسم النطاق الحقيقي، ثم أعد تسمية الملف إلى `CNAME` إذا كنت تستخدم النشر من الفرع.

## مهم
الموقع الحالي Static Site، لذلك لا يحتاج إلى PHP أو قاعدة بيانات لكي يعمل.
نموذج الاتصال الموجود حالياً تجريبي؛ عند الرغبة في استقبال الرسائل فعلياً يجب ربطه بخدمة بريد/نموذج أو Backend.

## قبل الإطلاق
- استبدال صور المعاينة بصور الجمعية الحقيقية.
- إضافة رقم الهاتف والبريد الإلكتروني.
- إضافة رابط صفحة Facebook.
- مراجعة اسم الجمعية المكتوب في جميع الصفحات.
- تحديث `sitemap.xml` باسم النطاق الحقيقي.
